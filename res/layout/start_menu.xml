<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
     >

    <!-- android:background="@drawable/custom_button" -->
    
    <Button
        android:id="@+id/showLogin_btn"
        android:layout_width="150dp"
        android:layout_height="100dp"
        android:layout_alignParentTop="true"
        android:layout_marginTop="30dp"        
        android:onClick="showLogin"
        android:text="login/register" />
    
    
        <Button
        android:id="@+id/showTestChord_btn"
        android:layout_width="wrap_content"
        android:layout_height="100dp"
        android:layout_alignBottom="@+id/showLogin_btn"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/showLogin_btn"
        android:layout_toRightOf="@+id/showLogin_btn"
        android:onClick="showTestChord"
        android:text="test chord" />


</RelativeLayout>

