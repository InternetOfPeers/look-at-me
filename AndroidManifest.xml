<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dreamteam.lookme"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- Necessary to chord framework -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!--
	Optional: A node cannot receive a UDP broadcast if it is in LCD-off status. 
	Therefore, if developers want the node to discover other nodes while the 
	application is running, the node’s status should be set to LCD-on.
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.dreamteam.lookme.MainActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.dreamteam.lookme.StartMenuActivity"
            android:label="@string/title_activity_test_chord" >
        </activity>

        <!-- Entry for RegisterActivity.class -->
        <activity
            android:name="com.dreamteam.lookme.RegisterActivity"
            android:label="Register New Account" >
        </activity>
        <activity
            android:name="com.dreamteam.lookme.SocialListActivity"
            android:label="@string/title_activity_social_list" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <service
            android:name="com.dreamteam.lookme.service.CommunicationService"
            android:enabled="true" >
            <intent-filter>
                <action android:name="com.dreamteam.lookme.service.CommunicationService.SERVICE_START" />
                <action android:name="com.dreamteam.lookme.service.CommunicationService.SERVICE_STOP" />
                <action android:name="com.dreamteam.lookme.service.CommunicationService.SERVICE_BIND" />
            </intent-filter>
        </service>
    </application>

</manifest>
