<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dreamteam.lookme"
    android:versionCode="1"
    android:versionName="0.5" >

    <!-- Necessary to chord framework -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!--
	Optional: A node cannot receive a UDP broadcast if it is in LCD-off status. 
	Therefore, if developers want the node to discover other nodes while the 
	application is running, the node’s status should be set to LCD-on.
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.dreamteam.lookme.SplashActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:label="@string/title_splash_activity"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.dreamteam.lookme.SocialActivity"
            android:label="@string/title_social_activity" >
        </activity>
        <activity
            android:name="com.dreamteam.lookme.ProfileActivity"
            android:label="@string/title_profile_activity" >
        </activity>
        <activity
            android:name="com.dreamteam.lookme.EditProfileActivity"
            android:label="@string/title_profile_activity" >
        </activity>
        <activity
            android:name="com.dreamteam.lookme.ManageInterestActivity"
            android:label="@string/title_manage_interest_activity" >
        </activity>
        <activity
            android:name="com.dreamteam.lookme.MessagesActivity"
            android:label="@string/title_messages_activity" >
        </activity>
        <activity
            android:name="com.dreamteam.lookme.FeelingsActivity"
            android:label="@string/title_feelings_activity" >
        </activity>
        <activity
            android:name="com.dreamteam.lookme.SettingsActivity"
            android:label="@string/title_settings_activity" >
        </activity>

        <service
            android:name="com.dreamteam.lookme.service.impl.BusinessLogicServiceImpl"
            android:enabled="true" >
            <intent-filter>
                <action android:name="com.dreamteam.lookme.service.BusinessLogicOperationsImpl.SERVICE_START" />
                <action android:name="com.dreamteam.lookme.service.BusinessLogicOperationsImpl.SERVICE_STOP" />
            </intent-filter>
        </service>
    </application>

</manifest>